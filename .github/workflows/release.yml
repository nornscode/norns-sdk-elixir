name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

# No MIX_ENV pin: hex.publish builds docs with ex_doc, a dev-only dep.
env:
  ELIXIR_VERSION: "1.18.4"
  OTP_VERSION: "27.3"

jobs:
  hex-publish:
    name: Publish to Hex
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ env.ELIXIR_VERSION }}
          otp-version: ${{ env.OTP_VERSION }}

      - run: mix deps.get
      - run: mix compile --warnings-as-errors
      - run: mix test

      - name: Publish package
        env:
          HEX_API_KEY: ${{ secrets.HEX_API_KEY }}
        run: |
          if [ -z "$HEX_API_KEY" ]; then
            echo "HEX_API_KEY is not configured. Skipping publish."
            exit 1
          fi
          mix hex.publish --yes
